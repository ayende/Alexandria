<Window x:Class="Alexandria.Client.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:Views="clr-namespace:Alexandria.Client.Views"
		Title="Alexandria">
	<Border Padding="10">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<Views:MyBooks DataContext="{Binding}" Grid.Column="0" Grid.Row="0"/>
			<Views:Queue DataContext="{Binding}" Grid.Column="1" Grid.Row="0"/>

			<Views:Recommendations DataContext="{Binding}" Grid.Row="1" Grid.ColumnSpan="2"/>

			<Views:Subscription DataContext="{Binding Path=SubscriptionDetails}" Grid.Column="0" Grid.Row="2"/>

			<Views:Search Grid.Row="2" Grid.Column="1" DataContext="{Binding}" />
			
		</Grid>
	</Border>
</Window>