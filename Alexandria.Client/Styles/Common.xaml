<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Converters="clr-namespace:Alexandria.Client.Infrastructure"
                    xmlns:Controls="clr-namespace:Alexandria.Client.Controls">
    
    <Converters:TimeSpanToHumanReadableStringConverter x:Key="TimespanConverter" />

    <Style TargetType="{x:Type Controls:HorizontalItemsControl}">
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ItemsControl">
                    <Border BorderBrush="Black"
                            BorderThickness="2"
                            CornerRadius="15"
                            Padding="10"
                            Margin="0,0,10,0">
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>